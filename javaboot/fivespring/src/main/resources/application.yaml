cors:
  originPatterns: http://localhost:8080,http://localhost:3000,https://develop.javaspring.com.br
spring:
  datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/bank?useTimezone=true&serverTimezone=UTC
      username: root
      password: admin123
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
      dialect: org.hibernate.dialect.MySQLDialect
      show-sql: false

springdoc:
  pathsToMatch:  /api/**/v1/**
  swagger-ui:
    use-root-path: true